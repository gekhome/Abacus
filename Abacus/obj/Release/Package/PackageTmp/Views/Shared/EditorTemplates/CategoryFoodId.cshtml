@using System.Collections
@using Abacus.Models
@using Abacus.DAL
@model int

@(Html.Kendo().DropDownListFor(m => m)
    .AutoBind(true)
    .DataValueField("ΚΑΤΗΓΟΡΙΑ_ΚΩΔ")
    .DataTextField("ΚΑΤΗΓΟΡΙΑ")
    .OptionLabel("Επιλογή...")
    .DataSource(source =>
    {
        source.Read(read =>
        {
            read.Action("FoodCategories_Read", "Admin");
        });
    })
)
@Html.ValidationMessageFor(m => m)