@using System.Collections
@using Abacus.Models
@using Abacus.DAL
@model int

@(Html.Kendo().DropDownListFor(m => m)
    .AutoBind(false)
    .DataValueField("ΜΕΣΗΜΕΡΙΑΝΟ_ΚΩΔ")
    .DataTextField("ΜΕΣΗΜΕΡΙΑΝΟ")
    .OptionLabel("Επιλογή...")
    .DataSource(source =>
    {
        source.Read(read =>
        {
            read.Action("GetMealsNoon", "Admin").Data("filterMealPlans");
        })
        .ServerFiltering(true);
    })
)
@Html.ValidationMessageFor(m => m)