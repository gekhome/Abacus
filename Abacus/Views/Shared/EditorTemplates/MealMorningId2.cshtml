@using System.Collections
@using Abacus.Models
@using Abacus.DAL
@model int

@(Html.Kendo().DropDownListFor(m => m)
    .AutoBind(false)
    .DataValueField("ΠΡΩΙΝΟ_ΚΩΔ")
    .DataTextField("ΠΡΩΙΝΟ")
    .OptionLabel("Επιλογή...")
    .DataSource(source =>
    {
        source.Read(read =>
        {
            read.Action("GetMealsMorning", "Station");
        })
        .ServerFiltering(true);
    })
)
@Html.ValidationMessageFor(m => m)